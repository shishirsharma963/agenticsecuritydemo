"""Application security: pentest remediation tracking and DAST.

    pentest  ingest findings -> assign owner + SLA -> track to closure
    dast     runtime checks (auth, headers, error leakage) against a target

Both are stdlib and synthetic; the value is the operating model (own it, gate it,
prove it), not the depth of any one tool.
"""
